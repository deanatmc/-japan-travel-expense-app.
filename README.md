# 日本旅遊記帳 App v4

1. 在 Supabase SQL Editor 執行 `schema-update.sql`。
2. 將所有檔案上傳到 GitHub Repository 根目錄。
3. GitHub Pages 重新部署。
4. 可先匯入 `latest-expenses-import.csv`。

功能：單筆新增、Excel/CSV 匯入預覽、日期序號轉換、搜尋、篩選、刪除、清空、匯出 Excel。

注意：目前 anon 可讀寫刪除；網站公開時建議之後加入 Supabase Auth。
